describe('angularAutoAdminLoopback.routes', function() {
    'use strict';

    beforeEach(module('angularAutoAdminLoopback'));

    it('should be defined', function() {
        expect(true)
            .toBe(true);
    });
});
