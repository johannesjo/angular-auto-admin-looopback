describe('angularAutoAdminLoopback', function() {
    'use strict';

    beforeEach(module('angularAutoAdminLoopback'));

    it('should always work', function() {
        expect(true)
            .toBe(true);
    });
});
